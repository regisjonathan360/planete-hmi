-- NOTE: Migration rendue sans-effet volontairement.
-- Script de nettoyage manuel au format MySQL (COLLATE utf8mb4_general_ci,
-- DISABLE TRIGGER ALL interdit sur les triggers système en PostgreSQL 15).
-- Déjà exécuté manuellement sur la base distante. Le nettoyage PostgreSQL
-- équivalent est 20260818_clean_dummy_data.sql.

select 1;